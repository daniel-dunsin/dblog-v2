import React from 'react'
import { connect } from 'react-redux';

const mapStateToProps = (state) => {

}
function Login() {
    return <section className='bg-gray-500 min-h-screen'>

    </section>
}

export default connect(mapStateToProps)(Login);